{
  "private": true,
  "name": "toy-tree",
  "version": "1.0.0",
  "bin": {
    "toy-tree": "src/bin.js"
  },
  "scripts": {
    "test": "jest",
    "format": "prettier --write \"**/*.{js,ts,json,md}\""
  },
  "dependencies": {
    "chalk": "^4.0.0",
    "meow": "^7.0.1"
  },
  "devDependencies": {
    "husky": "^4.2.3",
    "jest": "^26.0.1",
    "lint-staged": "^10.1.2",
    "prettier": "^2.0.2"
  },
  "jest": {
    "setupFiles": [
      "./jest.setup.js"
    ]
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "all"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,ts,json,md}": [
      "prettier --write",
      "git add"
    ]
  }
}
